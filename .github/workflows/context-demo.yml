name: Context Demo
on: push
jobs:
  context-demo:
    runs-on: ubuntu-latest
    steps:
      - name: Use contxt variable
        run: |
          echo "This workflow is triggered by ${{github.actor}}"
          echo "This repo name is ${{github.repository}}"
