name: Hello world
on:
  push:
    branches:
      - main
      - master
      - develop
      - "release/**"
  pull_request:
    types: 
      - opened
    branches: 
      - master
      
jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Build Setup
        run: echo "A demo build"

      

 