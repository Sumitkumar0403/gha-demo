name: Hello world
on:
  push:
    branches:
      - main
      - master
      - develop
      - "release/**"
  pull_request:
    branches:
      - master
      
jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Build Setup
        run: echo "Hello ${{secrets.DOCKER_PASS}}" 
 
